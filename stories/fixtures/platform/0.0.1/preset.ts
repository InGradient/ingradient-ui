/**
 * platform 0.0.1 preset placeholder.
 * Phase 4 에서 실제 Preset object 로 교체된다 (docs/plan/storybook-restructure-phase-4-presets.md).
 */
export const platformV001Preset = {
  id: 'platform-0.0.1',
  service: 'platform' as const,
  version: '0.0.1',
}
