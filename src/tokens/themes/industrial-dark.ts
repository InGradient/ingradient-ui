import type { TokenCategory } from '../density/types'

/**
 * industrial-dark — platform / edge 기본 분위기. 현재 dark mode 와 동일 (override 없음).
 */
export const industrialDarkTheme: TokenCategory = {
  cssVars: {},
}
